apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  type: NodePort
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80        # Port the service exposes
      targetPort: 80  # Port the container listens on
      nodePort: 30080 # Port to access on localhost